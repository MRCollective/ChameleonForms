@model ChildViewModel

<chameleon-form action="@Url.Action("PostChildViewModel")?tagHelpers=true">
    <form-section>
        <field for="ChildField" />
        <field for="SomeEnum" />
    </form-section>
    <form-navigation>
        <submit-button add-class="child-model" label="Submit" />
    </form-navigation>
</chameleon-form>
