@using Humanizer
@using ChameleonForms.Example.Views.Comparison.EditorTemplates
@{
    Layout = "~/Views/Comparison/EditorTemplates/_Layout.cshtml";
    var attrs = ViewData.SetupFieldAttrs();
}
@Html.DropDownList("", Enum.GetValues(typeof(ComparisonController.MembershipType)).Cast<ComparisonController.MembershipType>().Select(x => new SelectListItem{Text = x.Humanize(), Value = x.ToString()}), attrs)
