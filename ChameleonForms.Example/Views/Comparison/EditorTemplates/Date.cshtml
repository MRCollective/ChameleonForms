@{
    ViewData["type"] = "date";
    var attrs = new Dictionary<string, object>();
    if (!string.IsNullOrEmpty(ViewData.ModelMetadata.DisplayFormatString))
    {
        attrs["data-val-format"] = ViewData.ModelMetadata.DisplayFormatString.Replace("{0:", "").Replace("}", "");
    }
    ViewData["attrs"] = attrs;
}
@await Html.PartialAsync("~/Views/Comparison/EditorTemplates/HtmlInput.cshtml")
