@*

Layout template shared by all editor templates (except EditorSection.cs). 

This can be used to add any application-specific surrounding HTML. 

Note: EditorSection.cshtml and EditorExtensions methods provide a mechanism 
for displaying input elements with surrounding HTML and label and validation
elements

*@
@RenderBody()